--- net-snmp-5.1/agent/mibgroup/host/hr_system.c.orig	Tue Mar  9 11:47:30 2004
+++ net-snmp-5.1/agent/mibgroup/host/hr_system.c	Tue Mar  9 12:01:06 2004
@@ -271,6 +271,7 @@
 #define setutent setutxent
 #define getutent getutxent
 #define endutent endutxent
+#define pututline pututxline
     struct utmpx   *utmp_p;
 #else
     struct utmp    *utmp_p;
@@ -286,7 +287,7 @@
                current user */
             if (kill(utmp_p->ut_pid, 0) == -1 && errno == ESRCH) {
                 utmp_p->ut_type = DEAD_PROCESS;
-                pututxline(utmp_p);
+                pututline(utmp_p);
                 continue;
             }
             ++total;
